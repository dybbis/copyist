(function() {
    'use strict';

    angular
        .module('cmd.mode.controller', ['cmd.core'])
        .controller('Mode', Mode);

    Mode.$inject = ['$scope'];
    function Mode($scope) {
        var vm = this;

    };

})();
